const initialValue = {
  bun: null,
  mains: [],
  sauces: []
}

// this reducer contains list of ingredients in current burger constructor
export default function burgerIngredientsReducer(state = initialValue, action) {
  return state;
}
