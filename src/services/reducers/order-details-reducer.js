const initialValue = {
  number: 0,
  ingredients: [],
  status: null
}

// this reducer contains current order data
export default function orderDetailsReducer(state = initialValue, action) {
  return state;
}
