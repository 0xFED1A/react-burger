const initialValue = {
  buns: [],
  mains: [],
  sauces: []
}

// this reducer cotains list of available ingredients fetched from server
export default function burgerConstructorReducer(state = initialValue, action) {
  return state;
}
